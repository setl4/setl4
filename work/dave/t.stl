    define('test(set)')                   :(test.end)

test

    out('test powerset')
    out('input')
    show(set)
    out('output')
    show(power(set))
                                            :(return)

test.end

    test(new('set'))
    test(new('set a'))
    test(new('set a b'))
    test(new('set a b c'))

end
