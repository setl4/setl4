* lambda expressions

 s = "loop  output = le(i = i + 1,20) i :s(loop);end"
* c = code(s)
*  :(loop)

        define('lambda(x,f)')           :(lambda.end)

lambda

        code = "$x '.' $f; apply($f,$x);(end)
end
