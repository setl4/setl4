-include "setl4.sbl"
-include "util.sbl"


	expr = 
	primes = reduce(integers.new(n),
.	 expr)
	define('isprime(p)')			:(isprime.end)

*	Test if _p_ is prime

isprime

	integers = integers.new(p)
	i = int.new(2,p)
	
	exists(integers,'eq(remdr(p,=i))')
*	not there exists 2 < i < n such that remdr(p,i) is zero.
	define('primes(n)')			:(primes.end)

*	Return primes less than or equal to _n_

primes



