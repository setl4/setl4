*   Copyright 2017, David Shields
*   Licensed under the MIT license.

*   Program to align the goto fields in a program.

copy

	output = line
	align = 48
	margin = align - 8

loop

	line = input			:f(end)
	line '*'			:s(copy)
	line break(':')			:f(copy)

*	Here if ':'. See if after column 40.

	iline = line
	iline break(':') . first ':' rem . last = :f(copy)
	lt(size(first),margin)		:s(copy)

	first = trim(first)
	last span(' ') =
	line = rpad(trim(first),align) ':' last :(copy)
end
