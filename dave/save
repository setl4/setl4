    define('show.save(s)')                            :(show.save.end)

*   _Show.save_ saves the _index and _this_ fields of its argument set.

show.save

    push(show.stack,set.index(s))
                                                       :(return)

show.save.end

